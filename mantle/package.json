{
  "name": "smt-mantle-bridge",
  "version": "6.1.0",
  "private": true,
  "description": "SMTAgentRegistry — on-chain identity + reputation + AI decision record for SMT on Mantle (Turing Test 2026).",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network mantleSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network mantle"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
