# tests/qa — QA traceability (operator is QA; framework from Session C)

> **What's here now:** a *traceability matrix* auto-generated from the real test suite —
> inert CSVs (pytest ignores them), so they can't break the green run. Session E added
> the first non-smoke test types (integration / e2e / unit) for the daemon loop.

## Files (regenerate: `python3 tests/qa/generate_matrix.py`)
- `epics.csv` — EPIC-A…J ↔ Sessions A–J + status + case count.
- `test_cases.csv` — one row per real (parametrized) test: `tc, epic, type, severity(S1-S4), node, status`.
- `traceability.csv` — test-case counts per epic × type.

**Today:** 166 test cases (A:1, B:71, C:66, D:20, **E:8**). Session E split: e2e×3,
integration×3, unit×2; A–D remain smoke. Severity (regenerated by the rubric below —
`generate_matrix.py` is now the single source of truth, superseding the earlier
hand-count): S1×34, S2×123, S3×9.

## Severity rubric (Claude assigns; operator owns Priority)
- **S1 Critical** — money/data/security: fee-floor bypass, wrong-direction order, HARD-BLOCK bypass, JUDGE/direction-quality.
- **S2 High** — major feature dead, no workaround: entry/exit/reward/bandit broken.
- **S3 Medium** — broken with workaround: one persona degrades, others carry.
- **S4 Low** — cosmetic / docs / logging.

## The test pyramid (what we OWE — build alongside Sessions E/F)
- `unit/` — pure functions (reward terms, KDE, bandit math, regime_bucket). **STARTED** (Session E: cold-start defaults, position_pct).
- `integration/` — module-to-module (context↔tracker, bandit decision-gate). **STARTED** (Session E ×3; smoke A–D live here today).
- `regression/` — lock known-good PnL/WR baselines per version. **PENDING.**
- `invariant/` — property-based (fee floor never bypassed, symmetry rule #8, no NaN). **PENDING.**
- `e2e/` — full `Daemon.cycle` paper-trade. **STARTED — Session E** (cycle fires all components + writes the new exp schema + PARTIAL_CLOSE latch).
- backtest — the validation gates (DSR/PBO/FDR/CPCV). **PENDING — Session F.**

## Also pending (Session C design, not yet built)
- `conftest.py` that writes `tests/qa/test_runs/run_<UTC>.csv` (expected vs actual per TC).
- **Discord webhook C** — post per-run summary (pass/fail counts, deltas vs last run, failing TCs). *Needs the channel URL from operator.*
- `BRD.csv` (REQ-NN business requirements) + `stories.csv` (US-NN) for full BRD→Epic→Story→TC→Run traceability.
- Per-session policy (→ PLAN.md acceptance criteria): each session adds ≥1 test of each new test-type in its scope.
